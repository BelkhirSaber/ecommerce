# Ignorer les scripts postinstall des dépendances (évite l'erreur Husky)
ignore-scripts=true

# Utiliser le registry npm officiel
registry=https://registry.npmjs.org/

# Activer le package-lock pour une installation plus stable
package-lock=true